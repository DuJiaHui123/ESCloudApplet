<!--pages/map/map.wxml-->
<view>
  <map 
    style="width: 100%; height: 100vh;" 
    subkey="{{mapKey}}"
    show-location
    longitude="{{longitude}}"
    latitude="{{latitude}}"
    scale='13'
    markers="{{markers}}">
  </map>
</view>
